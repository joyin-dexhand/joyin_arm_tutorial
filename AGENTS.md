# 项目说明（ZCode 项目记忆）

本仓库为 JoyArm 机械臂教程文档站点，使用 MkDocs + Material 主题构建。

## 写作规范

### 表格
- **所有表格的单元格默认居中**（水平 + 垂直）
  - Markdown 表格统一用 `:---:` 对齐符（每列都写）
  - 项目已在 `docs/stylesheets/extra.css` 用 CSS 全局强制居中，无需依赖手写对齐符，但编写时仍应保持 `:---:` 习惯，确保源码与渲染一致
  - HTML 表格（含 `rowspan`/`colspan` 合并单元格）同样居中，`<td>`/`<th>` 无需额外写 `align`
- 新建/修改表格时无需再向用户确认是否居中，默认即居中

### 标题编号
- H1（章标题）：不加序号
- H2：阿拉伯数字 `1, 2, 3`（每章内从 1 重新编号）
- H3：`父H2.序号`，如 `2.1`
- H4+：依次类推 `2.1.1`
- `mkdocs.yml` 侧边栏的章序号用中文数字（第一章、第二章…），使用篇无章号
