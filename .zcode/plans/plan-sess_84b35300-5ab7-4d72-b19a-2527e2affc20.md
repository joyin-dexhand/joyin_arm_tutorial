退出规划模式以执行以下操作：

1. **停止 mkdocs serve 后台进程**（释放 8000 端口）
2. **讲解部署到 Web 的方案**（只读回答，无需改文件）

部署方案要点（会在停止进程后详细说明）：
- 仓库已有 `.github/workflows`（GitHub Actions），最契合的方式是 **GitHub Pages**：用 Actions 构建 mkdocs 站点并发布到 `https://<用户名>.github.io/<仓库名>/`，别人输入该网址即可访问，免费且无需服务器
- 备选：Cloudflare Pages / Vercel / 自有服务器 + Nginx 等