# 第十四章 用户接口

> 📌 **概述**：本章介绍机械臂对外的各类用户交互接口：包括 UI 界面、语音识别与合成、相机标定与图像识别解析、NFC 卡、UWB 以及体感设备。

<!-- ✍️ 写作指引（HTML 注释，不会渲染到线上；完稿后可保留或删除）

【本章定位】应用篇·集成章，讲机械臂"对外"的人机/环境交互接口，为 [第十五章](chapt15_project.md) 综合项目提供输入输出手段。
【参考模板】各节统一用"方案对比表 + 接入示例"风格（参考 quickstart.md / chapt1_intro.md 的对比表）。
【固定骨架】
  · 第 1 行：H1 章标题（章号用汉字）；第 3 行须是「> 📌 **概述**：……」blockquote 摘要
    （不加标题级别，一句话式概括全章、加粗关键词、点明各节逻辑流、点出末节实践）。
  · 正文按 ## → ### → #### 展开，每个顶级 ## 之间用 `---` 分隔。
  · 💡 建议补一节「## 7 本章实践：多模态用户接口集成」对齐全教程"节末实践"惯例，对应脚本 joyarm_code/chapt/chapt14_ui_demo.py。
【全局写作规范】（详见 AGENTS.md）
  · 简洁、清晰、对新手友好，避免跳跃。
  · 表格一律 `:---:` 居中（每列都写），无需向用户确认。
  · 标题编号：H2 阿拉伯数字每章从 1 重新编号；H3 为"父H2.序号"；章号用汉字。
  · 代码：优先用"脚本引用句式"——「对应脚本位于 `joyarm_code/chapt/chapt14_ui_demo.py`，使用说明见脚本头部注释。」
  · 可复用核心库 joyarm_code/joyarm_core/：vision（视觉）/ viz（可视化/UI）/ joyarm / ros2。
  · 图示：配图放 docs/images/chapt14_ui/（各接口示意/标定图），未就绪用 ![图片占位：描述]()。
  · callout：> 💡 直觉/口诀  ｜  > 📌 重要说明/跨章引用  ｜  > ⚠️ 警告/注意。
  · 跨章引用用相对链接（如 [第十二章](chapt12_teleop.md) 体感/VR、[第十五章](chapt15_project.md) 综合项目）。
  · 未知参数/型号用 *待补充*。
  · 不写"本章小结"——第 3 行概述已替代它。
【本章可引用的脚本/库】chapt14_ui_demo.py（待建）、joyarm_core/apps/vision.py（规划）、viz.py（待建，建议 quickstart/ 或 joyarm_core/apps/）、joyarm/joyarm.py、joyarm_ros2_ws/src/joyarm_node（joyarm_node 包）。
-->

## 1 UI界面

<!-- ✍️ 本节写作要点：
  · 目标：讲清机械臂控制/监控的图形界面设计——布局、关键控件、状态显示与指令下发。
  · 给"界面布局示意"（图占位）+ "关键功能表"（功能|交互方式|对应后端|说明）。
  · 引用 viz.py（待建，建议 quickstart/ 或 joyarm_core/apps/），用脚本引用句式。
  · 收尾 > 💡：好的 UI 把复杂能力封装成一两个按钮，降低使用门槛。
-->

## 2 语音识别与合成

<!-- ✍️ 本节写作要点：
  · 目标：讲清语音交互（ASR 识别 + TTS 合成）的接入与适用场景。
  · 一张"语音方案对比表"（方案|本地/云端|延迟|准确率|隐私|备注）。
  · 给一个最小接入示例（命令词 → 机械臂动作）。
-->

## 3 相机标定与图像识别解析

<!-- ✍️ 本节写作要点：
  · 目标：讲清相机标定（内参 + 手眼标定）与图像识别解析（经典视觉 + VLM）两块。
  · 已有两条要点（手眼标定 eye-in-hand/eye-to-hand；经典视觉检测/位姿估计、VLM 理解），建议：
    - 一张"手眼标定对比表"（方式|相机位置|标定对象|适用场景|备注）；
    - 一张"经典视觉 vs VLM 对比表"（方法|输出|数据需求|实时性|可解释性|适用）。
  · 引用 joyarm_core/apps/vision.py，用脚本引用句式；给出"识别结果 → 末端目标位姿"的衔接（用到 [第三章](chapt3_ikine.md) IK）。
-->

* 手眼标定（eye-in-hand / eye-to-hand）
* 经典视觉（目标检测/位姿估计）、VLM 视觉理解

## 4 NFC卡

<!-- ✍️ 本节写作要点：
  · 目标：讲清 NFC 卡作为"身份/任务凭证"的接入与适用场景（刷卡启停、任务切换）。
  · 给接入示意 + "NFC 应用表"（场景|触发动作|读取信息|说明）。
  · 收尾 > 💡：NFC 适合"一卡一任务"的快捷调度。
-->

## 5 UWB

<!-- ✍️ 本节写作要点：
  · 目标：讲清 UWB 定位作为外部位姿输入（如跟踪移动目标、人机协同安全距离）。
  · 给"UWB 特性表"（精度|刷新率|作用范围|多标签|说明）+ 接入示意。
  · ⚠️ 提示：UWB 定位误差需结合 [第十一章](chapt11_safety.md) 安全距离防护。
-->

## 6 体感设备

<!-- ✍️ 本节写作要点：
  · 目标：讲清体感设备（数据手套、动捕、惯性手环等）作为人体动作输入的接入。
  · 一张"体感设备对比表"（设备|采集量|自由度|延迟|穿戴难度|典型用途）。
  · 收尾 > 📌：体感输入常配合 [第十二章](chapt12_teleop.md) 遥操作做"人动→机动"映射。
-->

## 7 本章实践：多模态用户接口集成

<!-- ✍️ 本节写作要点：
  · 目标：用脚本集成本章多种用户接口（如 UI + 语音 + 视觉）控制机械臂。对应脚本位于 `joyarm_code/chapt/chapt14_ui_demo.py`，使用说明见脚本头部注释。
  · 结构建议：接口选型 → 集成流程 → 运行结果（配 ![图片占位：多模态交互效果]()）。
-->
