# 六轴整臂

> 📌 **本章定位**：面向"想立刻用起来"的读者，无需理论基础，按操作即可让六轴整臂动起来。

<!-- ✍️ 写作指引（HTML 注释，不会渲染到线上；完稿后可保留或删除）

【本章定位】使用篇·操作章（"想立刻用起来"，只讲操作步骤，原理见基础篇第二章正运动学、第六章运动控制）。
【参考模板】对齐 quickstart.md（快速上手，使用篇/操作章最完整的范本）。
【固定骨架】
  · 第 1 行：H1 章标题（使用篇无章号）；第 3 行须是「> 📌 **本章定位**：……」blockquote 摘要
    （一句话式概括全章、加粗关键词、点明各节逻辑流）。
    ⚠️ 当前第 3 行是斜体纯文本，撰写时请改写为该 blockquote 格式（使用篇用"本章定位"标签）。
  · 正文按 ## → ### 展开，每个顶级 ## 之间用 `---` 分隔。
  · 使用篇以"操作步骤"为主体，不展开理论；多用步骤流程表。
【全局写作规范】（详见 AGENTS.md）
  · 简洁、清晰、对新手友好，避免跳跃。
  · 表格一律 `:---:` 居中（每列都写），无需向用户确认。
  · 标题编号：H2 阿拉伯数字每章从 1 重新编号；H3 为"父H2.序号"。
  · 代码：优先用"脚本引用句式"——「对应脚本位于 `joyarm_code/chapt/xxx.py`，使用说明见脚本头部注释。」
  · 可复用核心库 joyarm_code/joyarm/：arm（Arm 类）/ joyarm_rebot_dm（JoyArmRebotDM 类）/ fkine / ikine / control / teleop。
  · 图示：配图放 docs/images/arm_6dof/，未就绪用 ![图片占位：描述]()。
  · callout：> 💡 直觉/口诀  ｜  > 📌 重要说明/跨章引用  ｜  > ⚠️ 警告/注意。
  · 未知数值用 *待补充*；跨章引用用相对链接 [第N章](chapterN_M.md)。
  · 不写"本章小结"——第 3 行概述已替代它。
【本章可引用的脚本/库】arms/arm.py、arms/joyarm_rebot_dm.py、robotics/control.py、joyarm/apps/teleop.py。
-->

## 1 操作安全须知

> 🔒 **首次上电必读**：保持工作空间无人员与障碍物；先将机械臂调至安全姿态再上电；上电后切勿强行扳动关节；紧急情况立即按下急停或断电。

<!-- ✍️ 本节写作要点：
  · 已有 🔒 安全 callout，保留。建议补充一张"急停/断电/恢复流程"小表（步骤|操作|目的）。
  · 可加 > ⚠️ 提示：操作六轴整臂比单关节风险更大，务必先空载、低速、就近工作空间。
-->

## 2 整臂上电与零位标定

<!-- ✍️ 本节写作要点：
  · 目标：让读者安全完成整臂上电与零位标定。
  · 主体用步骤流程表（步骤|具体操作|操作目的|操作演示图），格式参考 quickstart §7。
  · 配图：上电接线图、零位标定示意，放 docs/images/arm_6dof/；未就绪用 ![图片占位：...]()。
  · 引用 arms/joyarm_rebot_dm.py（JoyArmRebotDM 类的初始化/标定方法），用脚本引用句式。
-->

## 3 整臂关节控制

<!-- ✍️ 本节写作要点：
  · 目标：在关节空间逐关节控制整臂（对比单关节控制，强调六个关节的协同）。
  · 建议一张"位置/速度模式"对比表（模式|适用场景|关键参数|注意事项）。
  · 原理留到第六章，此处只给操作；引用 arms/arm.py 与 chapt 脚本。
  · 收尾可加 > 💡：关节控制是末端控制的基础。
-->

## 4 末端位置控制

<!-- ✍️ 本节写作要点：
  · 目标：在操作层面让读者用末端位姿控制整臂（输入 xyz/姿态 → 机械臂自动到位）。
  · 对比"末端控制 vs 关节控制"（控制对象|输入量|直观性|适用场景），点明底层走的是正运动学（原理见第二章）。
  · ⚠️ 必须提示工作空间边界与奇异点附近可能无法到位（原理见第四章）。
  · 引用 robotics/fkine.py / arms/arm.py，配 chapt 脚本。
-->

## 5 示教与轨迹复现

<!-- ✍️ 本节写作要点：
  · 目标：操作层面的"记录→回放"两步流程（示教模式录轨迹 → 回放复现）。
  · 用一张"数据记录→回放"流程表（步骤|操作|目的）。
  · 引用 joyarm/apps/teleop.py；原理与更多示教方式见第十二章。
  · 收尾可加 > 💡：示教复现是最快的"编程"方式。
-->
